# Skill Registry

## Project Conventions

- `AGENTS.md` — source of truth for stack conventions (Next.js App Router + strict TypeScript + Tailwind + Supabase), naming, testing commands, and architecture constraints.
- `DOCS/PROYECTO GANNT.md` — architecture blueprint with domain rules (domino scheduling, business-day calculations, DAG dependencies, print/export split).

## Project-Level Skills

- `frontend-design` — trigger: building/styling frontend pages/components with distinctive visual direction.
- `ui-ux-pro-max` — trigger: UI/UX structure, accessibility, interaction quality, and visual decision-making.
- `vercel-react-best-practices` — trigger: React/Next.js performance, rendering, bundle, and data-fetching optimization.

## User-Level Skills (Deduped)

- `branch-pr` — PR creation and branch workflow support.
- `go-testing` — Go testing patterns (including Bubbletea-focused flows).
- `issue-creation` — structured GitHub issue creation workflow.
- `judgment-day` — dual adversarial review workflow.
- `skill-creator` — authoring and packaging new skills.

## Notes

- No runtime project manifests (`package.json`, `go.mod`, `pyproject.toml`, `Cargo.toml`) were detected at repository root during initialization.
- Current repository state is documentation/skill-infrastructure focused; executable app source is not present in this snapshot.
